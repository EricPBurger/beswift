<div class="container">
  <h2>Costumes</h2>
  <div class="row">
    <% @costumes.each do |costume| %>
      <div class="col-md-4">
        <%= render "costume-card", costume: costume %>
      </div>
    <% end %>
  </div>
</div>
